中科浩电内部培训
Python第一课

1.交互模式
>>> _
* 说一下交互模式的回复上一条指令 Alt + p

2.打印Helloworld
输入这条语句即可
注意点：
* print() 函数(逗号会变成空格)
* 字符串使用""标记
* 变量长度不固定
* 没有;号

3.输入
input("Enter you code")

4.数据类型
整形、任意大小
浮点、1.24 x 10^3  = 1.24e3
字符串 使用双引号或单引号括起来，如果需要一起使用，可以使用转意符'\'
多行字符串，可以使用不转意符"r"
布尔量

后续还有列表、字典、元组

5.运算
+ - * /
注意点：
* 不提供++、--这样的自加1操作

6.列表
二位数组+链表
使用reverse()和sort()可以给list里的数据排序,但是这只对整形有意义

7.元组
a = (2)
和列表一样的访问方法，但是不能更改数据内容
a = (1,)申请一个元组

8.条件语句
if 判断语句:
    XXXXX
else:
    YYYYY

9.循环语句
for i in range(p):
    XXXXXX

